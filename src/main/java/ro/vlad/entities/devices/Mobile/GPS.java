package ro.vlad.entities.devices.Mobile;

import ro.vlad.entities.Item;

public class GPS extends Item {
    public GPS(String manufacturer, String model) {
        super(manufacturer, model);
    }
}
